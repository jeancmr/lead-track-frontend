# LeadTrack

1. Clone repository
2. Clone .env.template file and rename it as .env
3. Execute `yarn install` command to add dependecies
4. Verify backend is running
5. Execute `yarn dev` command to open development server
